import React from 'react';
// import moduleName from '../css';

const Loader = props => (
   <div>
      <h4>....................</h4>
      <i className="fa fa-refresh loader"></i>
   </div>
)

export default Loader;